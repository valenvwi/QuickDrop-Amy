<div class="navbar navbar-expand-sm navbar-light navbar-lewagon">
  <div class="container-fluid">
    <div class="navbar-brand">
      <%= image_tag "Black logo.png" %>
    </div>
    <%# Drop Down %>
    <a class="nav-link" id="navbarDropdown" href="#"  data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
      <button class="navbar-toggler bi bi-box-seam" style="font-size: 2rem;" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <%# <span class="navbar-toggler-icon"></span> %>
      </button>
    </a>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto">
        <% if user_signed_in? %>
          <%# Drop Down %>
          <% if current_user.driver? %>
            <li class="nav-item dropdown">
            <div class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
              <%= link_to "Get new job", driverindex_path, class: "dropdown-item" %>
              <%= link_to "View pending job", driveracceptedorders_path, class: "dropdown-item" %>
              <%= link_to "All Completed job", drivershow_path, class: "dropdown-item" %>
              <%= button_to"Log Out", destroy_user_session_path, method: :delete, class: "dropdown-item" %>
            </div>
          </li>
          <% else %>
          <li class="nav-item dropdown">
            <div class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
              <%= link_to "New Order", root_path, class: "dropdown-item" %>
              <%= link_to "View My Orders", orders_path(@orders), class: "dropdown-item" %>
              <%= button_to"Log Out", destroy_user_session_path, method: :delete, class: "dropdown-item" %>
            </div>
          </li>
          <% end %>
        <% else %>
          <li class="nav-item">
            <%= link_to "Login", new_user_session_path, class: "nav-link" %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</div>
