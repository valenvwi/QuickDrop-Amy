<div class="background-container">
  <div class="text-center m-2">
    <h2>Log in</h2>
  </div>
  <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name), data: { turbo: :false }) do |f| %>
    <div class="form-inputs px-3 pt-3">
      <%= f.input :email,
                  required: false,
                  autofocus: true,
                  input_html: { autocomplete: "email" } %>
      <%= f.input :password,
                  required: false,
                  input_html: { autocomplete: "current-password" } %>
      <%= f.input :remember_me, as: :boolean if devise_mapping.rememberable? %>
    </div>
    <div class="form-actions px-3 pb-4">
      <%= f.button :submit, "Log in" %>
    </div>
  <% end %>
</div>

<%# <%= render "devise/shared/links" %>


<footer class="mx-3 my-5">
  <i class="fa-solid fa-graduation-cap" style="color: #ff731d;"></i>
  <span id="footer-font"> This is a demo of a student project and not a real business</span>
</footer>
